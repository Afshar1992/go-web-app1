apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: my-cluster
  region: us-east-1

managedNodeGroups:
- name: spot-ng
  instanceTypes: ["t3.small", "t3a.small", "t2.small"]
  spot: true
  minSize: 1
  maxSize: 1